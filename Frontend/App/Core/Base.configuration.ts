/// <reference path="../_all.d.ts"/>

import {IBaseConfiguration} from 'ct';

class BaseConfiguration implements IBaseConfiguration {
  static $inject: string[] = [];

  constructor() {}
}

export default BaseConfiguration;
